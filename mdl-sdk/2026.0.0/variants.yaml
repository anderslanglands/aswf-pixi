c_stdlib:
  - if: linux
    then: sysroot

c_stdlib_version:
  - if: linux
    then: "2.17"

python:
  - 3.11
  - 3.12
  - 3.13

c_compiler_version:
  - if: target_platform == "osx-arm64"
    then: 22.1.8
cxx_compiler_version:
  - if: target_platform == "osx-arm64"
    then: 22.1.8
