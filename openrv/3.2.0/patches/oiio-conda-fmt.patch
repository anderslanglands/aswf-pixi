diff --git i/cmake/dependencies/oiio.cmake w/cmake/dependencies/oiio.cmake
index 157cdbe..8b9cce2 100644
--- i/cmake/dependencies/oiio.cmake
+++ w/cmake/dependencies/oiio.cmake
@@ -139,0 +140 @@ LIST(APPEND _configure_options "-DOIIO_BUILD_TOOLS=OFF" "-DOIIO_BUILD_TESTS=OFF"
+LIST(APPEND _configure_options "-Dfmt_ROOT=$ENV{PREFIX}")
@@ -203 +204,3 @@ ELSE()
-    "${_cpu_count}"
+    # OIIO's generated Windows build can link OpenImageIO_Util before all of
+    # its object files are ready when CMake drives it in parallel.
+    "1"
