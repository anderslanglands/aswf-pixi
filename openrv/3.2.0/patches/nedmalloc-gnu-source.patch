--- a/src/pub/nedmalloc/CMakeLists.txt
+++ b/src/pub/nedmalloc/CMakeLists.txt
@@ -26,6 +26,12 @@
 ENDIF()
 
 IF(RV_TARGET_LINUX)
+  TARGET_COMPILE_DEFINITIONS(
+    ${_target}
+    PRIVATE _GNU_SOURCE
+  )
+
   SET(THREADS_PREFER_PTHREAD_FLAG
       TRUE
   )
