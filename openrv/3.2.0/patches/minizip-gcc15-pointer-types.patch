diff --git a/src/pub/minizip/CMakeLists.txt b/src/pub/minizip/CMakeLists.txt
index 29c9d85b..e4aa8cb9 100644
--- a/src/pub/minizip/CMakeLists.txt
+++ b/src/pub/minizip/CMakeLists.txt
@@ -29,6 +29,13 @@ TARGET_LINK_LIBRARIES(
   PUBLIC ZLIB::ZLIB
 )
 
+IF(RV_TARGET_LINUX)
+  TARGET_COMPILE_OPTIONS(
+    ${_target}
+    PRIVATE "-Wno-error=incompatible-pointer-types"
+  )
+ENDIF()
+
 IF(RV_TARGET_WINDOWS)
   TARGET_COMPILE_OPTIONS(
     ${_target}
